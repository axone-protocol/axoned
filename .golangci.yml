version: "2"
linters:
  default: none
  enable:
    - asciicheck
    - bidichk
    - bodyclose
    - copyloopvar
    - cyclop
    - depguard
    - dupl
    - durationcheck
    - errcheck
    - errname
    - errorlint
    - exhaustive
    - forbidigo
    - funlen
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - godot
    - gomodguard
    - goprintffuncname
    - gosec
    - govet
    - ineffassign
    - lll
    - makezero
    - modernize
    - nakedret
    - nestif
    - nilerr
    - nilnil
    - noctx
    - nolintlint
    - prealloc
    - predeclared
    - promlinter
    - revive
    - staticcheck
    - tagliatelle
    - tparallel
    - unconvert
    - unparam
    - unused
    - wastedassign
    - whitespace
  settings:
    cyclop:
      max-complexity: 20
    depguard:
      rules:
        test-no-assertion-frameworks:
          files:
            - "**/*_test.go"
          deny:
            - pkg: github.com/stretchr/testify
              desc: "testify is forbidden in tests, use GoConvey."
            - pkg: gotest.tools
              desc: "gotest.tools is forbidden in tests, use GoConvey."
            - pkg: github.com/onsi/ginkgo
              desc: "ginkgo is forbidden in tests, use GoConvey."
            - pkg: github.com/onsi/gomega
              desc: "gomega is forbidden in tests, use GoConvey."
    forbidigo:
      forbid:
        - pattern: '(^|[^[:alnum:]_])assert\.[A-Za-z_][A-Za-z0-9_]*\('
          msg: "assert-style helpers are forbidden; use GoConvey assertions."
        - pattern: '(^|[^[:alnum:]_])require\.[A-Za-z_][A-Za-z0-9_]*\('
          msg: "require-style helpers are forbidden; use GoConvey assertions."
        - pattern: '(^|[^[:alnum:]_])suite\.Run\('
          msg: "testify suite is forbidden; use GoConvey."
        - pattern: '(^|[^[:alnum:]_])suite\.Suite\b'
          msg: "testify suite is forbidden; use GoConvey."
    funlen:
      statements: 65
    godot:
      scope: declarations
    lll:
      line-length: 135
    tagliatelle:
      case:
        rules:
          json: snake
          yaml: snake
        use-field-name: true
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - lll
        source: ^//\s*go:generate\s
      - linters:
          - godot
        source: (noinspection|TODO)
      - linters:
          - gocritic
        source: //noinspection
      - linters:
          - errorlint
        source: ^\s+if _, ok := err\.\([^.]+\.InternalError\); ok {
      - linters:
          - dupl
          - funlen
        path: _test\.go
      - linters:
          - revive
        path: _test\.go
        text: "dot-imports:"
      - linters:
          - revive
        path: "util/*"
        text: "var-naming: avoid meaningless package names"
      - linters:
          - revive
        path: "types/*"
        text: "var-naming: avoid meaningless package names"
      - linters:
          - cyclop
        path: (.+)_test\.go
    paths:
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  uniq-by-line: false
formatters:
  enable:
    - gci
    - goimports
  settings:
    gci:
      sections:
        - standard
        - default
        - blank
        - dot
        - prefix(github.com/cometbft/cometbft)
        - prefix(cosmossdk.io)
        - prefix(github.com/cosmos/cosmos-sdk)
        - prefix(github.com/cosmos/interchain-security)
        - prefix(github.com/cosmos/ibc-go)
        - prefix(github.com/axone-protocol/axoned)
      custom-order: true
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
